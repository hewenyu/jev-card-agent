# 实际验证记录

本文件区分真实调用、公开 Arena 对局和本地可控测试。验证日期按 UTC 记录；终端环境时区可能显示前一天。密钥、Bot 账户资料、原始牌局数据库和 provider 响应均不公开，`.env` 与 `data/` 被 Git 忽略。

## 真实 API

| 接口              | 请求                           | 观测结果                                                     | 可得结论                               |
| ----------------- | ------------------------------ | ------------------------------------------------------------ | -------------------------------------- |
| TypeSafe Jev      | `jev-1.13.0` Choice            | HTTP 200，返回相同模型，916 ms，371 input / 31 output tokens | Jev 请求协议和凭据可用                 |
| OpenPoker         | REST profile / active-game、WS | REST 200；WS 101 并收到 connected                            | 账户鉴权与连接可用                     |
| soleapi Responses | `gpt-6-astra`                  | HTTP 200，但返回模型为 `gpt-5.6-luna`，2896 ms               | Responses 可通信，**GPT-6 验证未通过** |
| soleapi Messages  | `claude-opus-5`，简短 OK 探针  | HTTP 200，返回相同模型，2166 ms                              | Messages 协议可用                      |

返回模型名是供应商所报告的元数据，不是底层模型身份的独立证明。项目拒绝返回模型与请求不一致的推理响应。

## 组合策略的真实验证

从真实 Arena 数据中冻结两个历史决策，在独立预算账本中使用完整 `HybridPolicy`：两次 Jev 路由均选择无需分析，耗时 895 ms / 836 ms，最终都为 fold。共两次 Jev 请求，估算费用 $0.000216846，零次 Claude 调用。此结果证明门控的直接路径可用，不能证明自然触发了完整三步链路。

另外做一次独立 Messages 扑克分析探针，最多 512 输出 token，12 秒 deadline。请求超过 deadline 后取消，usage 未知且账本保留费用预留；因为没有得到分析，没有调用 Jev reconsider。该探针不属于 Jev 自然路由，不计为端到端组合成功。

完整的 Jev → reasoning → Jev 分支、异常降级与模型不一致通过可控响应测试验证。仓库默认策略保持纯 Jev；服务器按部署配置启用组合策略，不宣称其真实扑克表现更好。

提高分析 deadline 到 30 秒后，另一次真实历史扑克样本使用 `claude-opus-5` Messages、`thinking: { type: 'adaptive' }` 和 1,200 输出 token 上限，分析成功，再由 `jev-1.13.0` reconsider 成功选择合法 check。共两次付费调用，使用独立有界账本。这证明启用思考的分析及 Jev 二次决策可用；它是显式 provider 探针，不等同于 Jev 自然门控触发的三步 Arena 实测。

## Arena 实测

首次纯 Jev 验证于 `2026-09-21T01:15:33Z` 启动，计划最长 30 分钟、模型预算 $1。观察到 10 手结算，其中 9 手具备完整起始/最终筹码，12 次提交全部 accepted。2 次 Jev 概率校验失败，均合法 fallback。官方 Choice 合同要求 choice 是最高概率选项且分布总和为 1，因此没有放松校验。

运行随后遇到 OpenPoker 可空 `hand_id` / `hand_seq` 字段，旧 envelope 校验退出。这次运行**不计为持续运行验收通过**。已针对实际可空消息修复并添加回归测试。

首手历史不完整不计收益，9 手可核对记录合计 -40 virtual chips。该小样本不用于评价长期扑克能力。原记录保留在本地私有数据库中，未因为修复而改写。

修复后的第二次纯 Jev Run 从 `2026-09-21T01:31:31.374Z` 运行至 `01:56:21.442Z`，共 **24 分 49 秒**。因开始服务器迁移而请求正常停止，等当前手牌结束并由官方 REST 确认已离桌；没有运行满原计划 30 分钟。数据库记录 33 手结束牌局，其中 32 手收益可核对、1 手历史不完整；Runtime 完成计数为 32 手。81 个行动全部 accepted，78 次 Jev 决策、3 次超时 fallback。可核对净筹码为 -890；已知 Jev 费用估算 $0.008594529，另有 3 次未知请求保留 $0.008064 预留。全部历史与费用账本保留并准备迁移到持久卷。

## 真实历史数据的本地核对

对运行数据库的一致备份执行了免费 baseline 重跑：20 个冻结决策、16 个选项一致、0 个错误、0 模型费用。原始 Run 未修改，重跑不产生替代收益。另用当前协议解析器检查了 784 条已保存事件及其中 339 个服务端状态哈希，全部通过；这是该次快照的数量，不代表持续运行的最终总数。

## 自动化和生产构建

已完成检查：ESLint 零 warning、Prettier、TypeScript strict、162 项 Vitest 测试、17 项 Playwright 测试、生产构建和仓库行数/秘密扫描通过。当前最长文件为 807 行；构建输出的 92 个文件也扫描了本地配置的真实秘密，未发现泄漏。全部 npm 依赖审计为零已知漏洞。其中包括免费补筹与持续入队、历史反馈截止点、稳定游标分页、公开读取权限与遗留浏览器令牌清理、缺失收益样本、稀疏 players 回放与右侧座位底牌可见性回归。

Docker 实际执行了干净 `npm ci` 和生产构建；容器以 UID 1000 运行，健康/页面/API 返回正常，只读写请求返回 403。写入临时持久卷标记后重启容器，标记仍存在。镜像中的 Runtime 与本地构建 SHA-256 一致；验证容器和临时卷已清理。具体命令：

```sh
npm ci
npm run check
npm run test:e2e
npm audit --omit=dev
```

截图来自构建后的 Node 服务与 SQLite 数据。合成 Demo 的 Overview、Replay、Experiments 及 390px 移动端已做实际截图检查；另使用无供应商凭据的本地只读查询服务检查真实运行数据。46 个真实行动回放节点的下注/弃牌状态无回退，决策信息截止点的公共牌和自己的底牌均与冻结输入一致。真实数据截图留在被忽略的私有目录，合成数据始终标为 Demo。

## GitHub 发布与服务器部署

提交 `674c874b7412391cfda071e3b12b51a42a9527ba` 的 [GitHub 镜像发布](https://github.com/hewenyu/jev-card-agent/actions/runs/35554105325) 已成功。镜像 manifest 为 `sha256:43c66bc586cedffde15b5380ff6e4c30c5fa7c9fb187fec90d210786213570dc`，已核实同时包含 `linux/amd64` 与 `linux/arm64`。构建不使用缓存，并重新拉取基础镜像；镜像后续版本与 digest 以对应工作流 Summary 为准。

服务器于 `2026-09-21T02:29:33Z` 使用 Docker Compose 启动，迁入原有 2 个 Run、44 手牌局、93 次决策、1,490 条事件与全部 93 条费用记录，SQLite 完整性检查通过。服务只绑定宿主 loopback，由 Nginx HTTPS 代理到公开域名。Bot 开启 `jev-reasoning`、Messages `claude-opus-5` adaptive thinking、自动补筹与持续入队；无手数/时长上限，累计模型估算预算为 $9，耗尽后采用合法 fallback。

`2026-09-21T02:32:57Z` 快照中，服务器 Run 已记录 7 手结算、7 次决策且无 fallback。新决策实际包含 10 手已核实历史摘要、问题模板版本和构建提交号。公网 `/health` 返回 200，匿名历史读取成功，进行中的底牌隐藏，匿名控制返回 403，错误令牌返回 401，编码 API 路径同样受保护。这是运行快照，不代表持续运行的最终数量。

首次服务器 Run 在 21 手后于 `2026-09-21T02:41:07Z` 遇到恢复快照哈希不一致，自动重启后恢复参赛。保留的原始字段定位到手牌间等待信息 `configured_delay_seconds`：服务端 Python 哈希使用浮点表示 `5.0`，旧 JavaScript 存档重编码为 `5`；仅恢复这项数值类型后，两份服务端哈希均精确匹配。协议解析现保留原数字 token，并在状态哈希和事件存档中使用，业务模型仍接收普通 number。校验规则没有放宽；无法核实的旧历史不猜测数值类型。相关恢复、嵌套快照及篡改检测由回归测试覆盖。

Compose 手动更新已实际等待当前手牌结束、用 OpenPoker REST 确认离桌，再拉取镜像并重建。更新前 Run 的结束时间早于新 Run 启动时间，历史与费用记录保留。在线备份还通过 SQLite 完整性检查，权限为 600。

公开界面地址：[openpoker.zve.ccwu.cc](https://openpoker.zve.ccwu.cc)。HTTPS 源站证书、Nginx 校验与 Certbot 模拟续期均通过。公网桌面浏览器已实际加载真实手牌、逐事件回放、Jev 候选概率和已接受动作。真实长 ID 与展开上下文在 390px 下暴露的两处溢出已修复，并新增先失败后通过的移动端回归。服务器统一使用 Compose 管理入口，已实际执行 start 验证保留原运行容器，并完成在线 SQLite 备份复制到宿主机；停止、重启和更新的排空顺序、失败时禁止替换及在线备份由 21 项隔离管理测试覆盖。

## 公开只读观战与数据刷新

公开前端已移除 Access settings、Bot 启停、策略配置、Demo 重置和实验创建入口。浏览器回归在后端仍报告可管理能力、浏览器遗留访问令牌的条件下遍历全部页面，确认没有管理表单，所有 API 请求均为匿名 GET，旧令牌已清理。评估页只读取已完成手牌对应的已有结果。

实时牌桌通过 SSE 接收公共快照；本地真实 HTTP/SSE 测试覆盖首帧、断线重连、订阅清理、慢客户端断开、私有字段隔离与动作去重。浏览器测试验证下注及派奖动画、连续动作排队、重复帧不重播、首次连接和重连建立动画基线、换手清理，以及 390px / reduced-motion。桌面和手机截图已检查，没有横向页面溢出。

概览、Run/手牌列表、选中回放详情和评估结果自动更新；新增手牌、相同手数下补写的结算、迟到决策和行动确认均有浏览器回归。刷新保留选择与回放游标，切 Run 后的旧响应不能覆盖当前记录。SSE 断线时保留较新快照，旧 HTTP 请求迟到不会让筹码或手牌倒退；断线后新发起的 HTTP 查询仍可更新牌桌。自动化均使用合成数据，不连接真实 Arena 或调用付费模型。

## 验证边界

- 服务器已真实自动参赛；自然故障与长期运行仍需持续监测。
- 没有证明长期收益、策略优势或排行榜名次。
- 真实 GPT-6 在当前代理上尚未验证；Messages 简短探针及 30 秒预算的扑克分析成功；12 秒预算的早期探针曾取消。
- 断线、重复 ACK、迟到响应和数据库冲突主要由本地协议测试验证，不能宣称生产运行自然经历了所有故障。
- 推理服务价格是可配置估算，实际费用以供应商账单为准。

## always / high 与同手 session 的有界真实探针（2026-09-21 UTC）

在 `04:00:52Z` 至 `04:01:26Z`，对服务器 `03:59:30Z` 一致备份中的一个真实 flop 决策执行**一次完整 `HybridPolicy.decide`**，未连接 OpenPoker、未提交动作。输入沿用该时点冻结局面和合法候选，并按 table、hand、时间与 table sequence 过滤后加入本手此前 1 次决策；两个模型请求均携带 session turn 2。独立私有 SQLite 账本上限为 $0.60，不修改正式运行的 $9 累计预算。

本次参数是 `REASONING_MODE=always`、Messages `claude-opus-5`、`thinking: { type: 'adaptive' }`、`output_config: { effort: 'high' }`、1,200 输出 token 上限、分析单次 30 秒、Hybrid 总计 35 秒。每个 provider 默认首次调用后最多 3 次重试，受同一 Hybrid deadline 和独立逐次预算预留约束。

| 实际请求          | 结果                                                                                        | 持久化费用                      |
| ----------------- | ------------------------------------------------------------------------------------------- | ------------------------------- |
| Messages 首次分析 | 30,004 ms 后取消；未取得 HTTP 响应、返回模型名、分析或 thinking                             | usage 未知，保留 $0.57936 预留  |
| Jev 首次决策      | 3,001 ms 后取消                                                                             | usage 未知，保留 $0.002688 预留 |
| Jev 第 1 次重试   | HTTP 200，返回 `jev-1.13.0`，905 ms，6,260 input / 73 output tokens；选择候选中的合法 check | 已知估算 $0.00026292            |

Messages 超时后，下一次分析的费用预留因 $0.60 上限被拒绝，因此没有发送第二个分析请求。分析输入为 8,888 字符，按现有保守规则预留 `8,888 × 4 + 16,384` input tokens；表内金额使用配置的 $10/M input、$50/M output 估算。不能只按最低 16,384 input tokens 估算一次分析所需预留。

最终结果为 `analysis_failed_jev_final`，完整流程耗时约 34 秒。3 次实际请求全部保存到 provider ledger，3 个 attempt 随最终决策保存，含首次失败及 `retryIndex=1` 的真实成功重试。已知估算费用 $0.00026292，加未核销预留 $0.582048，账本占用合计 $0.58231092；SQLite `quick_check` 为 `ok`，私有报告权限为 `600`。

这次验证证明同手 session 随请求发送、真实 Jev 重试、分析失败后仍由 Jev 选择合法候选，以及未知费用持续占用预算。**未验证本次服务接受 high 参数、返回 thinking，或成功完成分析 → Jev 的完整路径**；发送参数本身不等于服务支持。该探针使用 1,200 token / 35 秒，不能代表正式 4,096 token / 40 秒配置已通过。没有为取得成功结果重复执行第二次完整探针。

作为费用截止记录，服务器 `03:59:30Z` 的真实快照含 352 条费用记录，已知估算 $0.084695821、未核销预留 $0.01344；这是该快照的累计值，既不包含本次独立探针，也不代表之后的持续运行总额。以上记录补充早期验证，不改写此前 adaptive 路由的历史结果。

## Sonnet 5 / high 的有界真实探针（2026-09-21 UTC）

随后只读请求代理 `/v1/models` 返回 HTTP 200，列表包含 `claude-sonnet-5`。该列表只证明代理公布了这个 ID；因此以相同冻结样本执行一次独立探针，直接验证协议与时限。

`04:04:25Z` 至 `04:04:56Z`，使用与前次完全相同的冻结 flop 样本和同手 session，执行一次完整 Hybrid 决策。配置为 Messages `claude-sonnet-5`、adaptive thinking、`high` effort、4,096 输出 token、分析单次 30 秒、Hybrid 总计 40 秒；独立账本上限 $0.90，仍由 Jev 最终选择。未修改服务器配置，未连接牌桌或提交动作。

分析在 30,004 ms 后取消，未取得 HTTP 响应、实际模型名、分析文本或 thinking。该次 usage 未知，保留 $0.72416 预留；再次分析的预留被预算上限拒绝，未发送第二个 Messages 请求。Jev 随后首次调用即返回 HTTP 200、`jev-1.13.0`，耗时 878 ms，6,255 input / 73 output tokens，选择合法 check。完整流程约 30.94 秒，结果仍为 `analysis_failed_jev_final`。

两次实际请求及最终决策均已持久化：已知估算费用 $0.000262711、未知预留 $0.72416，总占用 $0.724422711。独立 SQLite `quick_check` 通过，私有报告权限为 `600`。**本次没有证明 Sonnet 5 接受 high 参数或能在配置时限内完成分析，不能作为正式 high 推理成功的依据。** 前次 Opus 失败记录保持有效，本次也未重复启动新的付费探针。

## 最新自动化检查（2026-09-21 UTC）

本轮完整 `npm run check` 通过，包括 ESLint、Prettier、TypeScript、212 项 Vitest 测试、生产构建与仓库检查；另有 22 项 Playwright 测试通过。仓库检查覆盖 148 个文件，最长文件 843 行；构建产物 100 个文件通过真实配置秘密扫描。上述自动化结果验证本地实现，不能替代前述真实 high 推理的成功证据。

正式推理模型保持 `claude-opus-5`，没有因模型列表或本次 Sonnet 探针切换配置。两次 high 分析超时均保留为未解决的真实服务验证限制。

## 账户筹码与其他玩家座位更新核对（2026-09-21 UTC）

只读检查服务器 `03:59:30Z` 的旧一致备份：2,645 条 `table_state.seats` 均包含 6 个座位；352 条 `your_turn.players` 摘要仅含 4、5 或 6 个玩家。与同桌此前最近的权威快照相比，30 条摘要省略了非空座位，其中 29 次为断线座位、1 次为 active 座位；另有摘要省略空座。摘要条目没有 `bet`、`folded` 或 `status`，因此不能用整个摘要数组替换已有牌桌状态。

本轮修复将 `your_turn.players` 按 seat 合并：更新服务端明确给出的筹码，保留未提及座位及缺失的下注、弃牌等字段；部分更新未给 name 时保留原名。完整 `table_state.seats` 仍作为权威快照替换，显式空座正常清空。这适用于全部玩家，其他玩家的座位筹码来自服务端消息，不代表其私有账户余额。

同一旧快照中，197 条 `hand_start` 均没有玩家筹码列表；202 条 `hand_result.final_stacks` 的键数为 3–6，只覆盖出现的座位。1,813 条 `player_action` 包含 `incremental` / `to_total` 两种金额口径，当前座位筹码应读取 `stack_after` / `player_stack_after` / `stack`，不能统一减去动作 `amount`。这些是历史消息结构检查，不是新的线上对局验收。

该备份没有 `rebuy_confirmed`、`auto_rebuy_scheduled` 或 `chips_skimmed` 事件，不能据此宣称已真实验收补筹到账、资金事件去重或补筹后的页面同步。官方补筹消息示例未保证唯一事件 ID、sequence 或交易时间；历史回填按原始事件行 ID 保持幂等，不把相同余额误当同一次补筹，也不由后续余额倒推未知的补筹前余额。

本轮资金与全座位修复的完整 `npm run check` 通过：ESLint、Prettier、TypeScript、31 个测试文件中的 **228 项 Vitest**、生产构建与仓库检查均通过；全量 **28 项 Playwright** 通过。仓库检查覆盖 160 个文件，最长文件 `src/runtime/runtime.ts` 为 869 行；104 个生产构建文件通过本地真实配置秘密扫描，无匹配。

新增自动化覆盖资金轮询与过期响应、补筹确认/冷却持久化、旧消息恢复、只读游标分页、刷新保留已加载历史，以及全部六个座位在动作、结算、离座、换人中的服务端筹码更新。动画金额不会用于推算玩家余额，旧 SSE/HTTP 响应不能覆盖新值。资金与座位回归使用合成数据，没有为验收主动触发真实补筹；正式账户余额可通过只读官方接口与页面快照核对。

发布前复查另发现重复补筹确认可能覆盖先前已核对的历史余额，现冲突更新仅补全未知字段，包括已知为零的数值也保持原样。新增回归先记录补筹后余额 1,500，再模拟买入后可用 500 / 在桌 1,000，重放同一 provider event ID 不改变原记录，新余额独立保存为 `balance_sync`。修复后的资金定向检查为 2 个文件、10 项测试通过。

## 庄家同步与新运行清理（2026-09-21 UTC）

同一真实旧备份中，197/197 条 `hand_start`、2,645/2,645 条 `table_state` 带顶层 `dealer_seat`，3/3 条 `resync_response` 在 `snapshot.dealer_seat` 中携带位置；352 条 `your_turn` 均未提供庄家，因此必须保留同手已知值。位置均为 0–5，其中三类含庄家消息的 0 座次数分别为 32、461、1；未观测到 null 或异常值，明确 null 清空属于防御性回归。

Live 和 Replay 共用明显的 D 标记及庄家座位/玩家文字，未知不猜测；按历史游标重建位置，不采用未来信息。换手/换桌清除旧位置，旧序列不回退，同水位权威恢复快照可更新。追加改动后的完整检查通过：**232 项 Vitest、30 项 Playwright**，含庄家更新、回放前后移动、空值清除和 390px 长姓名布局。

按所有者明确要求，部署将从干净的公开历史与牌桌状态开始。一份旧私有数据库的复制品已验证离线清理：7 个 Run、204 手牌局、6,136 条事件、352 次决策和动作，以及补入的评估/资金记录全部清空；352 条 `usage`、259 条 `provider_usage` 逐行散列与累计 98,135,821 nanos 账本占用保持不变。旧检查点、旧 session/对手统计来源和活动 Run 标记清除，只保留 `schema_version` 元数据。原始备份未修改，运行中、未离桌或存在有效租约时拒绝清理；这是副本验证，正式部署另行核对清理报告与新 Run。

## b146780 正式上线与干净运行验收（2026-09-21 UTC）

版本 `b146780` 已正式上线。`04:49:41Z` 完成实际服务器离线清理：旧 7 个 Run、265 手牌局、429 次决策、429 个动作和 7,826 条原始事件全部清零，旧牌桌恢复状态同步清除。旧数据库保留为权限 `0600` 的私有一致备份。429 条 `usage`、336 条 `provider_usage` 保持不变，累计账本占用仍为 **117,570,642 nanos（$0.117570642）**，清理没有重置已用费用或未知请求预留。

新 Run 于 `04:51:14Z` 开始；`04:52:47Z` 的核对快照包含 2 个 accepted 动作及 1 手已完成牌局。将展示数据与原始服务端事件逐项核对，六个座位筹码、庄家位置及 Bot 自己的 2 张底牌一致。官方 REST 与账户展示的离桌可用筹码 502、账户在桌筹码 1,980 一致；这些是该次核对值，不代表之后持续运行的余额，也不证明 REST 在桌快照与每个下注事件同步。

公网验收完成 19 项检查和 7 张截图，覆盖桌面及 390px 手机布局、自己的底牌、庄家、SSE 更新、匿名只读与无 JavaScript 错误。公开列表只包含清理后新 Run，旧历史仅在私有备份中保留。

新生产运行中的两次 Opus 分析仍各经历约 30 秒首次超时和受剩余 deadline 限制的约 7 秒重试超时，随后 Jev 成功决策。未取得思考返回，不能把 accepted 动作等同于推理分析成功。后续按所有者要求验证 Responses 候选 `gpt-6-astra` 与 `gpt-5.6-sol`，单次推理限时 10 秒；这次部署和界面验收本身不构成这些候选模型的真实身份或思考验证。

## 10 秒限制下的模型与思考模式对比（2026-09-21 UTC）

`04:58Z` 至 `05:06Z` 的比较使用同一份新 Run 冻结局面：context 为 1,619 字符，含指令与候选的完整模型输入为 2,449 字符。各项均采用 4,096 输出 token 上限、非流式响应、单次 10 秒期限，**每个模型/模式实际发送一次请求，不重试**，以比较单次耗时。正式 provider 的首次调用后最多 3 次重试规则没有因此改变。下面是 provider 分析探针，不是每个模型均完成了 Arena 行动或完整 Hybrid 链路。

| 模型 / 接口                      | 思考设置           | HTTP / 收到响应头 | 完整响应耗时      | 实际结果                                                                |
| -------------------------------- | ------------------ | ----------------- | ----------------- | ----------------------------------------------------------------------- |
| `gpt-6-astra` / 代理 Responses   | `effort=high`      | 未取得            | 10,003 ms 后取消  | 无实际模型、分析、思考或 usage                                          |
| `gpt-6-astra` / 代理 Responses   | `effort=none`      | 未取得            | 10,003 ms 后取消  | 无实际模型、分析、思考或 usage                                          |
| `gpt-5.6-sol` / 代理 Responses   | `effort=high`      | 未取得            | 10,005 ms 后取消  | 无实际模型、分析、思考或 usage                                          |
| `gpt-5.6-sol` / 代理 Responses   | `effort=none`      | 未取得            | 10,006 ms 后取消  | 无实际模型、分析、思考或 usage                                          |
| `gpt-5.6-luna` / 代理 Responses  | `effort=high`      | HTTP 404          | 717 ms 失败       | 该接口未提供模型结果                                                    |
| `gpt-5.6-luna` / 代理 Responses  | `effort=none`      | HTTP 404，740 ms  | 743 ms 失败       | 该接口未提供模型结果                                                    |
| `deepseek-flash` / 官方 Messages | `enabled` + `high` | HTTP 200，191 ms  | 10,006 ms 后取消  | 响应体未完成，无可用分析或 thinking                                     |
| `deepseek-flash` / 官方 Messages | `enabled` + `low`  | HTTP 200，206 ms  | 10,007 ms 后取消  | 响应体未完成，无可用分析或 thinking                                     |
| `deepseek-flash` / 官方 Messages | `disabled`         | HTTP 200，224 ms  | **3,773 ms 完成** | 实际模型 `deepseek-flash`，`end_turn`，分析 1,917 字符，thinking 0 字符 |

DeepSeek 的 `enabled/disabled` 是官方 Messages 思考开关；Responses 的 `none` 是本次向代理请求关闭思考的参数，超时未证实代理实际接受该设置。Luna 当时也未出现在代理模型列表中；HTTP 404 是本次接口结果，不证明所有服务均不支持这个名称。DeepSeek HTTP 200 响应头先到达不等于完整 JSON 已返回，因此 high / low 两项仍判超时。

DeepSeek 关闭思考的成功样本报告 835 input / 505 output tokens，`cache_read_input_tokens` 与 `cache_creation_input_tokens` 均为 0。按官方峰值非缓存输入 $0.30/M、输出 $1.20/M 保守估算为 **$0.0008565**；官方谷值可为一半，实际扣款仍以账单为准。

六次代理 Responses 请求都未取得 usage，每次保留 $0.4666 预留，含两个 HTTP 404，不把错误响应当作已确认免费。DeepSeek high / low 各保留 $0.0127692 未知费用预留。上述 9 次比较合计已知估算 $0.0008565、未核销预留 $2.8251384；预留是预算占用，不是已证明实际扣款，也不与其他早期探针或正式运行账本混算。

官方模型列表与[价格文档](https://api-docs.deepseek.com/quick_start/pricing)确认 `deepseek-flash` 对应 **DeepSeek-V4.1-Flash**，另提供 `deepseek-v4-pro`。不能自行把版本名称写成未经确认的请求 ID。该入口会将未知名称映射为 Flash，故仍须检查实际返回模型；协议和思考参数依据[Anthropic 兼容说明](https://api-docs.deepseek.com/guides/anthropic_api)与[Thinking Mode](https://api-docs.deepseek.com/guides/thinking_mode)。

只有 DeepSeek 关闭思考在本次单样本、10 秒配置内完整返回，**没有任何一项证明启用思考后成功返回 thinking**。关闭思考的 3.773 秒不是稳定延迟保证，也不能说明扑克决策质量更好。比较期间正式 Bot 已正常排空，服务器模型配置没有随探针自动切换；最终 provider、模型和思考设置由所有者查看这些数据后决定。

## DeepSeek 关闭思考的完整 Jev 链路验证（2026-09-21 UTC）

所有者根据上述比较选定 **DeepSeek-V4.1-Flash（`deepseek-flash`）、`DEEPSEEK_THINKING=disabled`、`REASONING_MODE=always`**，单次分析 10 秒，Hybrid 共用 40 秒期限，Jev 负责最终动作。这是选定配置；不将前一版本的上线记录当作本轮部署证据。

正式 provider → Jev 集成探针使用同一冻结局面，DeepSeek **4,411 ms** 返回，Jev **1,036 ms** 返回，完整链路 **5,450 ms**。实际模型分别为 `deepseek-flash` 与 `jev-1.13.0`，与请求一致；取得 2,269 字符分析、0 字符 thinking，Jev 选择合法 `fold`。两次请求均首次成功，无重试。这次验证覆盖完整决策链路，区别于上文 3,773 ms 的单 provider 分析探针；没有因此宣称已向 Arena 提交该探针动作。

DeepSeek 总输入 835 tokens，其中缓存命中 640、未命中 195，输出 569；Jev 输入 2,297、输出 75。两次请求估算合计 **$0.000841614**，未知费用预留为 0。实际扣款仍以供应商账单为准。这个成功样本证明当前配置可以完成正式 provider 与 Jev 串联，不证明长期延迟稳定或扑克策略收益。

另对 `05:20Z` 生产一致备份的复制品执行新 Store / LedgerMeter 迁移：原 1 个 Run、6 手牌局、8 次决策、8 个动作、87 条事件、453 条 usage 和 360 条 provider_usage 的原字段逐行哈希不变；3 个缓存用量列以增量方式加入，SQLite `quick_check` 通过。该检查未改动正在运行的数据库。

本轮最终验证通过：全量 Vitest **33 个测试文件、248 项测试**，全量 Playwright **33 项测试（16.1 秒）**，以及再次执行的 ESLint、TypeScript 和生产构建。E2E 包含自动刷新、关闭 thinking 的展示和 Live 首屏布局。仓库检查覆盖 167 个文件，最长文件为 869 行；106 个生产构建文件经 5 个真实私有密钥/令牌扫描，无匹配。

1440px 桌面与 390px 手机截图均已人工核对。默认 Overview 与品牌链接不变，进入 Live 后主牌桌位于首屏，桌面决策在右侧，手机依次为牌桌、决策和其他信息；账户与行动流仍由既有机制刷新。上述本地自动化、真实模型链路及生产备份迁移检查均不替代后续部署的线上验收。

## Overview 战绩聚焦验证（2026-09-21 UTC）

Overview 收敛为净收益、盈利手牌胜率、赛季积分三张统计卡片，以及积分 / 累计净收益切换曲线；不再放账户明细、Agent 资料、模型费用、决策数或最近手牌列表。账户与资金历史仍在 Live，逐手复盘仍在 Replay。双语 README 已同步。

统计来自新增匿名只读 `GET /api/runs/:id/performance`，按所选 Run 全历史计算，覆盖超出 100 条历史分页的记录。1,001 手和 1,001 个积分快照测试核对 500 点抽样的起终点与每个累计值；同时覆盖平局、负收益、未知收益、未结束手、真实零积分、同时间戳排序、跨 Run 隔离、rebuy 不计盈利、404 和公开写入拒绝。

本地 `npm run check` 通过：**254 项 Vitest**、ESLint、格式、TypeScript、生产构建与仓库检查；173 个文件中最长仍为 869 行。**39 项浏览器用例**经全量运行及过时文案断言修正后的单用例复测通过，涵盖统计自动刷新、失败保留旧值、聚焦恢复、旧 Run 延迟响应隔离、无样本、曲线切换和原有 Live / Replay / funding 功能。1440px 与 390px 截图已目检；Overview 只有统计和曲线，无详情列表及横向溢出。验证使用合成数据，不调用模型或入桌，不作为真实盈利证明；线上发布由无缓存 GitHub 镜像构建及手动 Compose 更新另行验收。

## 筹码口径修复与纯 Jev 数据采集（2026-09-21 UTC）

本轮目标是在备份并清理旧牌局历史后切换 `BOT_STRATEGY=jev`，持续收集真实对局数据。组合分析 provider 不在该策略调用链中；每次直接把冻结的可见局面、session、对手统计和近期已核实结果摘要交给 Jev。成功请求及解析后的响应、候选概率、模型用量、行动状态与结算继续落库；输入裁剪不删除原历史。上下文版本升级为 `visible-context-v4`，用于区分本轮下注与结算口径。数小时采集是运行目标，本节本地验证不表示已采足数据。

Live 和 Replay 对每个已入座玩家分别展示 Available 与 Bet，零下注保留；账户区分别展示离桌余额、可下注余额、当前街下注和官方在桌快照。2026-09-21 06:04 UTC 的生产核对中，WS 为 stack 1,960 / bet 10，REST 在桌快照为 1,970；同序列浏览器六座位与 WS 一致，说明账户快照不能直接替代实时可下注金额。新增合并逻辑优先较大 table_seq，SSE 停更而连接仍开的情况下，HTTP 轮询也能更新牌桌。

结算采用服务端 final_stacks 并清零 Bet，底池改标 Settled pot；换街清旧下注。Replay 按服务端贡献增量、明确下注或已知筹码差还原下注，恢复快照与随后保存的重放动作去重，防止重复累加。测试覆盖旧序列、权威恢复、结算完成状态、候选输入口径、自己与对手余额及公开只读页面。

全量 `npm run check`（**283 项 Vitest**）与 **44 项 Playwright** 通过，包含纯 Jev 实时会话文案、四金额字段独立刷新、六座位更新、SSE 停更与过时消息防回退。1440px 和 390px 布局截图已目检，手机侧边标签无裁切，底牌及下注可读。仓库检查中最长文件仍为 869 行，未发现已配置密钥。部署前旧 Bot 已在手牌边界停止并经 OpenPoker REST 确认离桌；一致 SQLite 备份已保存。随后所有者明确授权清空旧历史：部署时将离线清理旧 Run、牌局、决策、行动、事件、评估、资金展示与恢复检查点，但保留费用账本及未知预留。实际新版本上线与首批样本仍须另行核实，不能将本地检查当作生产采集证明。
